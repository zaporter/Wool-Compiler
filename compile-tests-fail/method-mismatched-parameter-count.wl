class A {
    x : int <- 0  ;

    y1 : int <-0;
    z : int <- x/y1;
    y : B <- new C;
    k : Str <- "pizza";
    b : int <- b;
    io : IO <- new IO;
         func() : int {
             8
         }
         main(x:int, y:boolean) : int {
             y:B<- new C;
             j:int<-0;
             k:int<-j;
             x:int<-x;
               {
                   y.func(io);
                   func();
               }
         }
}
class B{
         func(x : int, gamma:int) : boolean{
             x>5
         }
    
}
class C inherits B{

}
